# Krushi Sathi Environment Variables

# Google AI Studio API Key (Optional)
# Get your free API key from: https://makersuite.google.com/app/apikey
# Without this, the app will use template responses
AI_API_KEY=

# Database URL (Optional) 
# For persistent storage. Uses in-memory storage if not provided
DATABASE_URL=

# Application Settings (Automatically set in production)
NODE_ENV=development
PORT=3000